Custom Date Picker Powered with Angular.js.
Just a sample to show that I know something about Angular.js.

To see web-page in action serve all it resources from any local server as regular html page (it will not work if just open it as file in browser.)
Angular.js library reference is to Google's CDN.